#!/usr/bin/make -f
#export DH_VERBOSE = 1
#export DEB_BUILD_MAINT_OPTIONS = hardening=+all

%:
	dh $@

override_dh_auto_configure:
	dh_auto_configure -- --with-llvm=/usr/lib/llvm-11

override_dh_auto_build:
	dh_auto_build -- stage
	dh_auto_build -- all

override_dh_dwz:
