#!/usr/bin/make -f
#export DH_VERBOSE = 1
#export DEB_BUILD_MAINT_OPTIONS = hardening=+all

%:
	dh $@

override_dh_auto_test:
	dh_auto_build -- -C tests build
	dh_auto_test --no-parallel

override_dh_compress:
	dh_compress -Xexample -Xreference
